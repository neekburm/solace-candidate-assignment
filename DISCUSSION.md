Other features I would add given more time:

# Functional Tests:

Pagination behavior (buttons, page numbers, counts)
Search across all fields (names, city, degree, experience, specialties)
Sorting functionality with visual indicators
Debounce behavior verification
Error handling scenarios

# API Tests:

Direct curl commands to test the API endpoint
Edge cases like invalid page numbers, excessive limits, out-of-range pages
Data integrity (empty arrays, special characters)
Performance (query optimization, index usage)
Invalid page numbers, excessive limits, out-of-range pages

# UI/UX:

Responsive design on mobile/tablet
Visual states (hover effects, disabled buttons)
Table horizontal scrolling on narrow screens

# Accessibility:

Keyboard navigation
Screen reader compatibility
Focus states
Motion preferences